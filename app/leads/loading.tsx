import AppShell from "@/components/app-shell";

export default function LeadsLoading() {
  return (
    <AppShell>
      <main className="section">
        <div className="container">
          <span className="eyebrow">Lädt</span>
          <h1>Leads werden geladen.</h1>
        </div>
      </main>
    </AppShell>
  );
}