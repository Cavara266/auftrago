.header {
  position: sticky;
  top: 0;
  z-index: 3000;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  background: rgba(3, 7, 18, 0.96);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  box-shadow: 0 14px 40px rgba(0, 0, 0, 0.25);
}

.shell {
  width: min(1380px, calc(100% - 40px));
  min-height: 76px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 28px;
}

.logo {
  display: inline-flex;
  align-items: center;
  flex-shrink: 0;
  color: #ffffff;
  font-size: 24px;
  font-weight: 950;
  letter-spacing: -1px;
  text-decoration: none;
}

.logo::before {
  content: "🔥";
  width: 46px;
  height: 46px;
  margin-right: 11px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(248, 113, 113, 0.22);
  border-radius: 15px;
  background:
    radial-gradient(circle at 50% 35%, rgba(249, 115, 22, 0.3), transparent 60%),
    linear-gradient(145deg, rgba(69, 10, 10, 0.9), rgba(27, 12, 25, 0.95));
  box-shadow:
    0 10px 25px rgba(239, 68, 68, 0.16),
    inset 0 1px rgba(255, 255, 255, 0.08);
  font-size: 21px;
}

.logo span {
  color: #7dd3fc;
}

.desktopNav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 25px;
  min-width: 0;
  flex: 1;
}

.desktopNav a,
.navButton {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  border: 0;
  background: transparent;
  color: rgba(255, 255, 255, 0.72);
  font: inherit;
  font-size: 14px;
  font-weight: 750;
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  transition:
    color 160ms ease,
    transform 160ms ease;
}

.desktopNav a:hover,
.navButton:hover,
.active {
  color: #ffffff !important;
  transform: translateY(-1px);
}

.premiumLink {
  gap: 7px !important;
}

.premiumLink small {
  padding: 4px 7px;
  border-radius: 999px;
  color: #111827;
  background: linear-gradient(135deg, #fde047, #f59e0b);
  font-size: 9px;
  font-weight: 950;
}

.actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10px;
  flex-shrink: 0;
}

.searchButton,
.mobileButton,
.primaryButton {
  min-height: 46px;
  border: 1px solid rgba(255, 255, 255, 0.13);
  border-radius: 15px;
  color: #ffffff;
  font: inherit;
  font-size: 14px;
  font-weight: 900;
  cursor: pointer;
  transition:
    transform 160ms ease,
    filter 160ms ease,
    box-shadow 160ms ease;
}

.searchButton,
.mobileButton {
  width: 46px;
  min-width: 46px;
  padding: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(145deg, #38bdf8, #4f8df7 48%, #6d5dfc);
  box-shadow:
    0 12px 28px rgba(79, 70, 229, 0.3),
    inset 0 1px rgba(255, 255, 255, 0.25);
}

.primaryButton {
  padding: 0 19px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #38bdf8, #6366f1);
  text-decoration: none;
  box-shadow: 0 12px 30px rgba(79, 70, 229, 0.28);
}

.searchButton:hover,
.mobileButton:hover,
.primaryButton:hover {
  filter: brightness(1.08);
  transform: translateY(-2px);
}

.searchButton:active,
.mobileButton:active,
.primaryButton:active {
  transform: scale(0.95);
}

.mobileButton {
  display: none;
}

/* Desktop Dienstleistungsmenü */

.megaMenu,
.regionMenu {
  position: absolute;
  top: 76px;
  left: 0;
  right: 0;
  z-index: 2900;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  background:
    radial-gradient(circle at 15% 0%, rgba(56, 189, 248, 0.14), transparent 32%),
    radial-gradient(circle at 85% 5%, rgba(99, 102, 241, 0.17), transparent 34%),
    linear-gradient(145deg, rgba(3, 7, 18, 0.99), rgba(9, 17, 34, 0.99));
  box-shadow: 0 28px 70px rgba(0, 0, 0, 0.42);
  animation: menuReveal 180ms ease-out;
}

.megaMenuInner {
  width: min(1180px, calc(100% - 40px));
  margin: 0 auto;
  padding: 34px 0 38px;
  display: grid;
  grid-template-columns: 0.75fr 2.25fr;
  gap: 42px;
}

.megaIntro {
  position: relative;
  overflow: hidden;
  padding: 25px;
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 22px;
  background:
    radial-gradient(circle at 90% 10%, rgba(99, 102, 241, 0.2), transparent 38%),
    linear-gradient(145deg, rgba(15, 23, 42, 0.98), rgba(7, 13, 27, 0.98));
}

.megaIntro span,
.searchEyebrow {
  display: block;
  margin-bottom: 10px;
  color: #7dd3fc;
  font-size: 11px;
  font-weight: 950;
  letter-spacing: 0.18em;
  text-transform: uppercase;
}

.megaIntro h2 {
  margin: 0 0 12px;
  color: #ffffff;
  font-size: clamp(25px, 3vw, 38px);
  line-height: 1.03;
  letter-spacing: -1.5px;
}

.megaIntro p {
  margin: 0;
  color: rgba(203, 213, 225, 0.72);
  font-size: 13px;
  line-height: 1.65;
}

.megaIntro a {
  display: inline-flex;
  margin-top: 18px;
  color: #ffffff;
  font-size: 13px;
  font-weight: 850;
  text-decoration: none;
}

.megaGrid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 24px;
}

.megaGroup {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.megaGroup > strong {
  margin-bottom: 4px;
  color: rgba(255, 255, 255, 0.42);
  font-size: 11px;
  font-weight: 900;
  letter-spacing: 1px;
  text-transform: uppercase;
}

.megaGroup a {
  display: flex;
  align-items: center;
  gap: 9px;
  min-height: 40px;
  padding: 0 10px;
  border-radius: 11px;
  color: rgba(226, 232, 240, 0.86);
  font-size: 13px;
  font-weight: 700;
  text-decoration: none;
  transition:
    background 160ms ease,
    color 160ms ease,
    transform 160ms ease;
}

.megaGroup a:hover {
  color: #ffffff;
  background: rgba(56, 189, 248, 0.08);
  transform: translateX(3px);
}

.regionMenu {
  padding: 28px 0 34px;
}

.regionGrid {
  width: min(1000px, calc(100% - 40px));
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 12px;
}

.regionGrid a {
  min-height: 56px;
  padding: 0 18px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border: 1px solid rgba(255, 255, 255, 0.09);
  border-radius: 14px;
  color: #ffffff;
  background: rgba(255, 255, 255, 0.035);
  font-size: 13px;
  font-weight: 800;
  text-decoration: none;
}

.regionGrid a:hover {
  border-color: rgba(56, 189, 248, 0.35);
  background: rgba(56, 189, 248, 0.09);
}

/* Suche */

.searchOverlay {
  position: fixed;
  inset: 0;
  z-index: 6000;
  overflow-y: auto;
  padding:
    max(80px, env(safe-area-inset-top))
    max(20px, env(safe-area-inset-right))
    max(32px, env(safe-area-inset-bottom))
    max(20px, env(safe-area-inset-left));
  color: #ffffff;
  background:
    radial-gradient(circle at 10% 10%, rgba(56, 189, 248, 0.2), transparent 34%),
    radial-gradient(circle at 90% 15%, rgba(99, 102, 241, 0.22), transparent 35%),
    linear-gradient(180deg, #02050d, #07101f);
  backdrop-filter: blur(30px);
  -webkit-backdrop-filter: blur(30px);
  animation: searchReveal 180ms ease-out;
}

.closeSearch {
  position: fixed;
  top: max(18px, env(safe-area-inset-top));
  right: max(18px, env(safe-area-inset-right));
  z-index: 6100;
  width: 48px;
  height: 48px;
  padding: 0;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(255, 255, 255, 0.14);
  border-radius: 15px;
  color: #ffffff;
  background: linear-gradient(145deg, #38bdf8, #6366f1);
  box-shadow: 0 15px 34px rgba(79, 70, 229, 0.38);
  font-size: 24px;
  cursor: pointer;
}

.searchPanel {
  width: min(100%, 760px);
  margin: 0 auto;
}

.searchPanel h2 {
  max-width: 680px;
  margin: 0 0 30px;
  color: #ffffff;
  font-size: clamp(40px, 7vw, 68px);
  line-height: 0.98;
  letter-spacing: -3px;
}

.searchPanel form {
  margin-bottom: 22px;
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  gap: 12px;
}

.searchPanel input {
  width: 100%;
  min-height: 58px;
  padding: 0 18px;
  border: 1px solid rgba(125, 211, 252, 0.35);
  border-radius: 16px;
  outline: none;
  color: #ffffff;
  background: rgba(15, 23, 42, 0.72);
  font: inherit;
  font-size: 16px;
}

.searchPanel input:focus {
  border-color: #38bdf8;
  box-shadow: 0 0 0 4px rgba(56, 189, 248, 0.12);
}

.searchPanel form button {
  min-width: 116px;
  min-height: 58px;
  padding: 0 22px;
  border: 0;
  border-radius: 16px;
  color: #ffffff;
  background: linear-gradient(135deg, #38bdf8, #7c3aed);
  font: inherit;
  font-size: 14px;
  font-weight: 950;
  cursor: pointer;
}

.searchResults {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.searchResults a {
  min-height: 72px;
  padding: 13px 16px;
  display: grid;
  grid-template-columns: 46px minmax(0, 1fr) auto;
  align-items: center;
  gap: 13px;
  border: 1px solid rgba(255, 255, 255, 0.09);
  border-radius: 18px;
  color: #ffffff;
  background: linear-gradient(
    135deg,
    rgba(56, 189, 248, 0.9),
    rgba(99, 102, 241, 0.92)
  );
  text-decoration: none;
  box-shadow:
    0 14px 34px rgba(30, 64, 175, 0.16),
    inset 0 1px rgba(255, 255, 255, 0.18);
  transition:
    transform 160ms ease,
    filter 160ms ease;
}

.searchResults a:hover {
  filter: brightness(1.08);
  transform: translateY(-2px);
}

.searchResults a > span {
  width: 46px;
  height: 46px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(255, 255, 255, 0.17);
  border-radius: 14px;
  background: rgba(255, 255, 255, 0.1);
  font-size: 21px;
}

.searchResults strong {
  display: block;
  margin-bottom: 3px;
  font-size: 14px;
  font-weight: 950;
}

.searchResults small {
  display: block;
  color: rgba(15, 23, 42, 0.62);
  font-size: 11px;
  font-weight: 750;
}

.searchResults b {
  font-size: 18px;
  font-weight: 400;
}

/* Mobile Menü */

.mobileMenu {
  display: none;
}

@media (max-width: 1179px) {
  .shell {
    width: min(100% - 28px, 1380px);
    min-height: 70px;
    gap: 14px;
  }

  .desktopNav,
  .primaryButton {
    display: none;
  }

  .actions {
    margin-left: auto;
  }

  .mobileButton {
    display: inline-flex;
  }

  .megaMenu,
  .regionMenu {
    display: none;
  }

  .mobileMenu {
    position: fixed;
    inset: 70px 0 0;
    z-index: 2950;
    overflow-y: auto;
    padding:
      18px
      max(18px, env(safe-area-inset-right))
      max(26px, env(safe-area-inset-bottom))
      max(18px, env(safe-area-inset-left));
    display: grid;
    align-content: start;
    gap: 10px;
    background:
      radial-gradient(circle at 10% 0%, rgba(56, 189, 248, 0.14), transparent 30%),
      radial-gradient(circle at 90% 5%, rgba(99, 102, 241, 0.16), transparent 32%),
      linear-gradient(180deg, #050914, #081120);
    animation: menuReveal 180ms ease-out;
  }

  .mobileMenu a {
    min-height: 56px;
    padding: 0 17px;
    display: flex;
    align-items: center;
    border: 1px solid rgba(255, 255, 255, 0.09);
    border-radius: 16px;
    color: rgba(255, 255, 255, 0.92);
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.06),
      rgba(255, 255, 255, 0.025)
    );
    font-size: 14px;
    font-weight: 850;
    text-decoration: none;
    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.035),
      0 10px 28px rgba(0, 0, 0, 0.12);
  }

  .mobileMenu a::after {
    content: "→";
    margin-left: auto;
    padding-left: 14px;
    color: rgba(125, 211, 252, 0.9);
    font-size: 17px;
    font-weight: 900;
  }

  .mobileMenu a:hover {
    border-color: rgba(96, 165, 250, 0.38);
    background: linear-gradient(
      135deg,
      rgba(56, 189, 248, 0.13),
      rgba(99, 102, 241, 0.13)
    );
  }

  .mobileMenu a:last-child {
    margin-top: 8px;
    justify-content: center;
    border-color: rgba(96, 165, 250, 0.34);
    color: #ffffff;
    background: linear-gradient(135deg, #38bdf8, #6366f1);
    box-shadow:
      0 16px 36px rgba(79, 70, 229, 0.3),
      inset 0 1px rgba(255, 255, 255, 0.2);
  }

  .mobileMenu a:last-child::after {
    display: none;
  }
}

@media (max-width: 620px) {
  .shell {
    width: calc(100% - 22px);
    min-height: 66px;
  }

  .logo {
    font-size: 20px;
    letter-spacing: -0.9px;
  }

  .logo::before {
    width: 39px;
    height: 39px;
    flex-basis: 39px;
    margin-right: 9px;
    border-radius: 13px;
    font-size: 18px;
  }

  .searchButton,
  .mobileButton {
    width: 42px;
    min-width: 42px;
    height: 42px;
    min-height: 42px;
    border-radius: 13px;
  }

  .mobileMenu {
    inset: 66px 0 0;
    padding: 14px 13px max(22px, env(safe-area-inset-bottom));
    gap: 8px;
  }

  .mobileMenu a {
    min-height: 52px;
    padding: 0 15px;
    border-radius: 14px;
    font-size: 13px;
  }

  .searchOverlay {
    padding-top: 86px;
  }

  .closeSearch {
    width: 44px;
    height: 44px;
    border-radius: 14px;
  }

  .searchPanel h2 {
    margin-bottom: 24px;
    font-size: clamp(37px, 12vw, 54px);
    letter-spacing: -2.4px;
  }

  .searchPanel form {
    grid-template-columns: 1fr;
  }

  .searchPanel form button {
    width: 100%;
  }

  .searchResults a {
    min-height: 66px;
    grid-template-columns: 42px minmax(0, 1fr) auto;
    gap: 11px;
    border-radius: 16px;
  }

  .searchResults a > span {
    width: 42px;
    height: 42px;
    border-radius: 13px;
  }
}

@media (max-width: 380px) {
  .logo {
    font-size: 18px;
  }

  .logo::before {
    width: 36px;
    height: 36px;
    flex-basis: 36px;
    margin-right: 8px;
  }

  .actions {
    gap: 6px;
  }

  .searchButton,
  .mobileButton {
    width: 39px;
    min-width: 39px;
    height: 39px;
    min-height: 39px;
  }
}

@keyframes menuReveal {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes searchReveal {
  from {
    opacity: 0;
    transform: scale(0.985);
  }

  to {
    opacity: 1;
    transform: scale(1);
  }
}

/* =========================================================
   MOBILE: NUR ANBIETER, PREISRECHNER UND OFFERTE ANFRAGEN
   ========================================================= */

@media (max-width: 1179px) {
  .megaMenu,
  .megaMenuInner,
  .regionMenu,
  .regionGrid,
  .searchOverlay {
    display: none !important;
  }

  .mobileMenu {
    position: fixed !important;
    inset: 70px 0 0 0 !important;
    z-index: 5000 !important;
    width: 100% !important;
    height: calc(100dvh - 70px) !important;
    padding: 20px 16px !important;
    display: flex !important;
    flex-direction: column !important;
    gap: 12px !important;
    overflow-y: auto !important;
    background:
      radial-gradient(
        circle at 50% 0%,
        rgba(56, 189, 248, 0.12),
        transparent 35%
      ),
      linear-gradient(
        180deg,
        #060b18 0%,
        #09101f 100%
      ) !important;
  }

  .mobileMenu a {
    width: 100% !important;
    min-height: 64px !important;
    margin: 0 !important;
    padding: 0 20px !important;
    display: flex !important;
    align-items: center !important;
    border: 1px solid rgba(255, 255, 255, 0.1) !important;
    border-radius: 18px !important;
    background: rgba(255, 255, 255, 0.045) !important;
    color: #ffffff !important;
    font-size: 16px !important;
    font-weight: 850 !important;
    text-decoration: none !important;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.04),
      0 14px 32px rgba(0, 0, 0, 0.16) !important;
  }

  .mobileMenu a::after {
    content: "→" !important;
    margin-left: auto !important;
    color: #7dd3fc !important;
    font-size: 20px !important;
  }

  .mobileMenu a:last-child {
    border: 0 !important;
    color: #06101f !important;
    background:
      linear-gradient(
        135deg,
        #38bdf8 0%,
        #4f8df7 48%,
        #6366f1 100%
      ) !important;
    box-shadow:
      0 16px 38px rgba(79, 70, 229, 0.3),
      inset 0 1px 0 rgba(255, 255, 255, 0.25) !important;
  }
}

/* MOBILE-ACTIONS-CLEANUP-START */
@media (max-width: 1179px) {
  .searchButton,
  .premiumLink {
    display: none !important;
  }
}
/* MOBILE-ACTIONS-CLEANUP-END */


/* ===== PREMIUM HEADER 2026 ===== */

.header {
  position: sticky;
  top: 0;
  z-index: 3000;
  border-bottom: 1px solid rgba(148, 163, 184, 0.12);
  background:
    linear-gradient(
      180deg,
      rgba(4, 9, 20, 0.94) 0%,
      rgba(4, 10, 23, 0.90) 100%
    );
  backdrop-filter: blur(28px) saturate(150%);
  -webkit-backdrop-filter: blur(28px) saturate(150%);
  box-shadow:
    0 14px 42px rgba(0, 0, 0, 0.30),
    inset 0 -1px 0 rgba(255, 255, 255, 0.025);
}

.shell {
  width: min(1420px, calc(100% - 48px));
  min-height: 82px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 26px;
}

.logo {
  position: relative;
  display: inline-flex;
  align-items: center;
  flex-shrink: 0;
  color: #ffffff;
  font-size: 25px;
  font-weight: 950;
  letter-spacing: -1.15px;
  text-decoration: none;
  transition:
    transform 180ms ease,
    filter 180ms ease;
}

.logo:hover {
  transform: translateY(-1px);
  filter: brightness(1.08);
}

.desktopNav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
}

.desktopNav > a,
.navButton {
  position: relative;
  min-height: 44px;
  padding: 0 13px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  border: 0;
  border-radius: 13px;
  background: transparent;
  color: rgba(226, 232, 240, 0.82);
  font: inherit;
  font-size: 13px;
  font-weight: 780;
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  transition:
    color 160ms ease,
    background 160ms ease,
    transform 160ms ease;
}

.desktopNav > a:hover,
.navButton:hover {
  color: #ffffff;
  background: rgba(255, 255, 255, 0.055);
  transform: translateY(-1px);
}

.desktopNav > a::after,
.navButton::after {
  content: "";
  position: absolute;
  left: 14px;
  right: 14px;
  bottom: 5px;
  height: 2px;
  border-radius: 999px;
  background: linear-gradient(90deg, #38bdf8, #6366f1);
  opacity: 0;
  transform: scaleX(0.4);
  transition:
    opacity 160ms ease,
    transform 160ms ease;
}

.desktopNav > a:hover::after,
.navButton:hover::after,
.active::after {
  opacity: 1;
  transform: scaleX(1);
}

.premiumLink {
  gap: 7px !important;
}

.premiumLink small {
  padding: 4px 7px;
  border: 1px solid rgba(253, 224, 71, 0.28);
  border-radius: 999px;
  color: #111827;
  background: linear-gradient(135deg, #fde047, #f59e0b);
  box-shadow: 0 5px 15px rgba(245, 158, 11, 0.18);
  font-size: 8px;
  font-weight: 950;
  letter-spacing: 0.04em;
}

.priceCalculatorLink {
  min-height: 44px !important;
  padding: 0 14px !important;
  gap: 8px !important;
  border: 1px solid rgba(56, 189, 248, 0.18) !important;
  color: #f8fbff !important;
  background:
    linear-gradient(
      135deg,
      rgba(56, 189, 248, 0.10),
      rgba(99, 102, 241, 0.08)
    ) !important;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.045),
    0 7px 20px rgba(15, 23, 42, 0.14);
}

.priceCalculatorLink:hover {
  border-color: rgba(56, 189, 248, 0.40) !important;
  background:
    linear-gradient(
      135deg,
      rgba(56, 189, 248, 0.18),
      rgba(99, 102, 241, 0.14)
    ) !important;
  box-shadow:
    0 10px 28px rgba(56, 189, 248, 0.10),
    inset 0 1px 0 rgba(255, 255, 255, 0.07);
}

.priceCalculatorIcon {
  width: 22px;
  height: 22px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 7px;
  color: #ffffff;
  background: linear-gradient(135deg, #38bdf8, #6366f1);
  box-shadow: 0 6px 16px rgba(56, 189, 248, 0.22);
  font-size: 15px;
  font-weight: 900;
}

.actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 10px;
  flex-shrink: 0;
}

.searchButton {
  min-height: 46px;
  padding: 0 17px;
  border: 1px solid rgba(148, 163, 184, 0.15);
  border-radius: 15px;
  color: rgba(248, 250, 252, 0.94);
  background:
    linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.065),
      rgba(255, 255, 255, 0.025)
    );
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.055);
  font-size: 13px;
  font-weight: 850;
  transition:
    transform 160ms ease,
    border-color 160ms ease,
    background 160ms ease,
    box-shadow 160ms ease;
}

.searchButton:hover {
  transform: translateY(-1px);
  border-color: rgba(125, 211, 252, 0.32);
  background: rgba(56, 189, 248, 0.09);
  box-shadow: 0 9px 24px rgba(2, 132, 199, 0.08);
}

.primaryButton {
  min-height: 48px;
  padding: 0 20px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  border: 1px solid rgba(147, 197, 253, 0.30);
  border-radius: 16px;
  color: #ffffff;
  background:
    radial-gradient(
      circle at 20% 10%,
      rgba(255, 255, 255, 0.18),
      transparent 34%
    ),
    linear-gradient(135deg, #38bdf8 0%, #4f8df7 48%, #6366f1 100%);
  box-shadow:
    0 11px 30px rgba(59, 130, 246, 0.24),
    inset 0 1px 0 rgba(255, 255, 255, 0.30);
  font-size: 13px;
  font-weight: 900;
  text-decoration: none;
  transition:
    transform 160ms ease,
    filter 160ms ease,
    box-shadow 160ms ease;
}

.primaryButton:hover {
  transform: translateY(-2px);
  filter: brightness(1.06);
  box-shadow:
    0 16px 36px rgba(59, 130, 246, 0.32),
    inset 0 1px 0 rgba(255, 255, 255, 0.34);
}

.primaryArrow {
  width: 24px;
  height: 24px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.16);
  font-size: 15px;
  transition: transform 160ms ease;
}

.primaryButton:hover .primaryArrow {
  transform: translateX(2px);
}

@media (max-width: 1180px) {
  .shell {
    width: min(100% - 30px, 1420px);
    gap: 15px;
  }

  .desktopNav {
    gap: 1px;
  }

  .desktopNav > a,
  .navButton {
    padding-left: 9px;
    padding-right: 9px;
    font-size: 12px;
  }

  .priceCalculatorLink {
    padding-left: 10px !important;
    padding-right: 10px !important;
  }

  .primaryButton {
    padding-left: 15px;
    padding-right: 15px;
  }
}

/* ===== LUXURY HEADER FINAL ===== */

.header {
  position: sticky;
  top: 0;
  z-index: 3000;
  border-bottom: 1px solid rgba(125, 211, 252, 0.10);
  background:
    radial-gradient(
      circle at 72% -80%,
      rgba(59, 130, 246, 0.15),
      transparent 38%
    ),
    linear-gradient(
      180deg,
      rgba(3, 8, 19, 0.975),
      rgba(4, 10, 23, 0.955)
    );
  backdrop-filter: blur(28px) saturate(160%);
  -webkit-backdrop-filter: blur(28px) saturate(160%);
  box-shadow:
    0 18px 55px rgba(0, 0, 0, 0.30),
    inset 0 -1px 0 rgba(255, 255, 255, 0.025);
}

.shell {
  width: min(1440px, calc(100% - 56px));
  min-height: 88px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 28px;
}

.logo {
  position: relative;
  flex-shrink: 0;
  color: #ffffff;
  font-size: 26px;
  font-weight: 950;
  letter-spacing: -1.35px;
  text-decoration: none;
  text-shadow: 0 6px 24px rgba(56, 189, 248, 0.10);
}

.logo span {
  color: #7dd3fc;
}

.desktopNav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
}

.desktopNav > a:not(.priceCalculatorLink),
.navButton {
  position: relative;
  min-height: 46px;
  padding: 0 14px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  border: 0;
  border-radius: 14px;
  color: rgba(226, 232, 240, 0.78);
  background: transparent;
  font: inherit;
  font-size: 13px;
  font-weight: 790;
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  transition:
    color 180ms ease,
    background 180ms ease,
    transform 180ms ease;
}

.desktopNav > a:not(.priceCalculatorLink):hover,
.navButton:hover {
  color: #ffffff;
  background: rgba(255, 255, 255, 0.055);
  transform: translateY(-1px);
}

.priceCalculatorLink {
  position: relative;
  min-height: 52px;
  padding: 7px 15px 7px 9px;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  overflow: hidden;
  border: 1px solid rgba(125, 211, 252, 0.28);
  border-radius: 17px;
  color: #ffffff;
  background:
    radial-gradient(
      circle at 15% 10%,
      rgba(255, 255, 255, 0.14),
      transparent 32%
    ),
    linear-gradient(
      135deg,
      rgba(14, 165, 233, 0.16),
      rgba(79, 70, 229, 0.14)
    );
  box-shadow:
    0 12px 32px rgba(14, 165, 233, 0.10),
    inset 0 1px 0 rgba(255, 255, 255, 0.09);
  text-decoration: none;
  transition:
    transform 180ms ease,
    border-color 180ms ease,
    box-shadow 180ms ease,
    background 180ms ease;
}

.priceCalculatorLink::before {
  content: "";
  position: absolute;
  top: -70%;
  left: -45%;
  width: 60%;
  height: 240%;
  transform: rotate(18deg);
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.12),
    transparent
  );
  transition: left 420ms ease;
}

.priceCalculatorLink:hover {
  transform: translateY(-2px);
  border-color: rgba(125, 211, 252, 0.52);
  background:
    radial-gradient(
      circle at 15% 10%,
      rgba(255, 255, 255, 0.18),
      transparent 34%
    ),
    linear-gradient(
      135deg,
      rgba(14, 165, 233, 0.24),
      rgba(79, 70, 229, 0.22)
    );
  box-shadow:
    0 16px 38px rgba(14, 165, 233, 0.18),
    inset 0 1px 0 rgba(255, 255, 255, 0.12);
}

.priceCalculatorLink:hover::before {
  left: 115%;
}

.priceCalculatorSpark {
  width: 34px;
  height: 34px;
  flex: 0 0 34px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(186, 230, 253, 0.27);
  border-radius: 11px;
  color: #ffffff;
  background:
    linear-gradient(135deg, #0ea5e9, #4f46e5);
  box-shadow:
    0 7px 18px rgba(59, 130, 246, 0.28),
    inset 0 1px 0 rgba(255, 255, 255, 0.20);
  font-size: 15px;
}

.priceCalculatorText {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  line-height: 1.05;
}

.priceCalculatorText strong {
  color: #ffffff;
  font-size: 13px;
  font-weight: 900;
  letter-spacing: -0.15px;
}

.priceCalculatorText small {
  margin-top: 4px;
  color: rgba(186, 230, 253, 0.67);
  font-size: 8px;
  font-weight: 800;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 11px;
  flex-shrink: 0;
}

.searchButton {
  min-height: 50px;
  padding: 0 18px;
  border: 1px solid rgba(148, 163, 184, 0.16);
  border-radius: 16px;
  color: rgba(248, 250, 252, 0.91);
  background:
    linear-gradient(
      145deg,
      rgba(255, 255, 255, 0.065),
      rgba(255, 255, 255, 0.025)
    );
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.055);
  font-size: 13px;
  font-weight: 880;
  transition:
    transform 180ms ease,
    border-color 180ms ease,
    background 180ms ease;
}

.searchButton:hover {
  transform: translateY(-2px);
  border-color: rgba(125, 211, 252, 0.34);
  background: rgba(56, 189, 248, 0.09);
}

.primaryButton {
  min-height: 52px;
  padding: 0 19px 0 21px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  border: 1px solid rgba(186, 230, 253, 0.36);
  border-radius: 17px;
  color: #ffffff;
  background:
    radial-gradient(
      circle at 20% 0%,
      rgba(255, 255, 255, 0.24),
      transparent 36%
    ),
    linear-gradient(
      135deg,
      #38bdf8 0%,
      #4285f4 47%,
      #6366f1 100%
    );
  box-shadow:
    0 13px 34px rgba(59, 130, 246, 0.27),
    inset 0 1px 0 rgba(255, 255, 255, 0.34);
  font-size: 13px;
  font-weight: 920;
  text-decoration: none;
  transition:
    transform 180ms ease,
    filter 180ms ease,
    box-shadow 180ms ease;
}

.primaryButton:hover {
  transform: translateY(-3px);
  filter: brightness(1.07);
  box-shadow:
    0 19px 43px rgba(59, 130, 246, 0.36),
    inset 0 1px 0 rgba(255, 255, 255, 0.38);
}

.primaryArrow {
  width: 27px;
  height: 27px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.15);
  font-size: 15px;
  transition: transform 180ms ease;
}

.primaryButton:hover .primaryArrow {
  transform: translateX(3px);
}

@media (max-width: 1250px) {
  .shell {
    width: min(100% - 32px, 1440px);
    gap: 15px;
  }

  .desktopNav {
    gap: 1px;
  }

  .desktopNav > a:not(.priceCalculatorLink),
  .navButton {
    padding-left: 9px;
    padding-right: 9px;
    font-size: 12px;
  }

  .priceCalculatorText small {
    display: none;
  }

  .priceCalculatorLink {
    padding-right: 11px;
  }
}

/* ===== /LUXURY HEADER FINAL ===== */



/* ===== HEADER FINAL FIX 2026 ===== */

.header {
  position: sticky;
  top: 0;
  z-index: 3000;
  background:
    radial-gradient(circle at 72% -60%, rgba(59,130,246,.15), transparent 34%),
    linear-gradient(180deg, rgba(3,8,20,.98), rgba(4,10,23,.96));
  border-bottom: 1px solid rgba(125,211,252,.11);
  backdrop-filter: blur(28px) saturate(155%);
  -webkit-backdrop-filter: blur(28px) saturate(155%);
  box-shadow: 0 18px 50px rgba(0,0,0,.28);
}

.shell {
  width: min(1440px, calc(100% - 48px));
  min-height: 84px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
}

.desktopNav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
}

.desktopNav > a,
.navButton {
  min-height: 44px;
  padding: 0 13px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  border-radius: 13px;
  color: rgba(226,232,240,.82);
  background: transparent;
  font-size: 13px;
  font-weight: 800;
  text-decoration: none;
  transition: 160ms ease;
}

.desktopNav > a:hover,
.navButton:hover {
  color: #fff;
  background: rgba(255,255,255,.055);
  transform: translateY(-1px);
}

.priceCalculatorLink {
  min-height: 52px !important;
  padding: 7px 14px 7px 8px !important;
  display: inline-flex !important;
  align-items: center;
  gap: 10px !important;
  border: 1px solid rgba(96,165,250,.34) !important;
  border-radius: 17px !important;
  color: #fff !important;
  background:
    radial-gradient(circle at 15% 0%, rgba(255,255,255,.16), transparent 34%),
    linear-gradient(135deg, rgba(14,165,233,.18), rgba(79,70,229,.16)) !important;
  box-shadow:
    0 12px 30px rgba(37,99,235,.14),
    inset 0 1px 0 rgba(255,255,255,.10);
}

.priceCalculatorLink:hover {
  transform: translateY(-2px) !important;
  border-color: rgba(125,211,252,.55) !important;
  box-shadow:
    0 17px 38px rgba(37,99,235,.22),
    inset 0 1px 0 rgba(255,255,255,.14);
}

.priceCalculatorIcon {
  width: 34px;
  height: 34px;
  flex: 0 0 34px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border-radius: 11px;
  background: linear-gradient(135deg, #0ea5e9, #4f46e5);
  box-shadow: 0 7px 18px rgba(59,130,246,.30);
  font-size: 16px;
  font-weight: 900;
}

.priceCalculatorCopy {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  line-height: 1.05;
}

.priceCalculatorCopy strong {
  font-size: 13px;
  font-weight: 900;
}

.priceCalculatorCopy small {
  margin-top: 4px;
  font-size: 8px;
  font-weight: 800;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: rgba(186,230,253,.72);
}

.actions {
  display: flex;
  align-items: center;
  gap: 10px;
}

.searchButton {
  min-height: 48px;
  padding: 0 17px;
  border: 1px solid rgba(148,163,184,.16);
  border-radius: 15px;
  background: rgba(255,255,255,.035);
  color: #fff;
  font-weight: 850;
}

.primaryButton {
  min-height: 50px;
  padding: 0 20px;
  border-radius: 16px;
  color: #fff;
  background:
    radial-gradient(circle at 20% 0%, rgba(255,255,255,.23), transparent 36%),
    linear-gradient(135deg, #38bdf8 0%, #4285f4 48%, #6366f1 100%);
  box-shadow:
    0 14px 34px rgba(59,130,246,.28),
    inset 0 1px 0 rgba(255,255,255,.30);
  font-weight: 900;
}

@media (max-width: 900px) {
  .header {
    min-height: auto;
  }

  .shell {
    width: calc(100% - 24px);
    min-height: 68px;
    gap: 10px;
  }

  .desktopNav {
    display: none !important;
  }

  .logo {
    font-size: 21px;
  }

  .actions {
    gap: 8px;
  }

  .searchButton {
    min-height: 42px;
    padding: 0 12px;
    font-size: 0;
  }

  .searchButton::after {
    content: "⌕";
    font-size: 22px;
  }

  .primaryButton {
    min-height: 42px;
    padding: 0 13px;
    font-size: 12px;
  }

  .mobileButton {
    min-width: 42px;
    min-height: 42px;
  }

  .mobileMenu {
    position: absolute;
    top: 68px;
    left: 12px;
    right: 12px;
    display: grid;
    gap: 8px;
    padding: 12px;
    border: 1px solid rgba(255,255,255,.10);
    border-radius: 18px;
    background: rgba(4,10,23,.98);
    box-shadow: 0 20px 45px rgba(0,0,0,.34);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
  }

  .mobileMenu a {
    min-height: 48px;
    display: flex;
    align-items: center;
    padding: 0 15px;
    border-radius: 13px;
    color: #fff;
    text-decoration: none;
    font-weight: 850;
    background: rgba(255,255,255,.035);
  }

  /* doppelte Header-Schrift verhindern */
  .mobileMenu + *,
  .header + .header {
    display: none !important;
  }
}
